{
  "name": "jx-mini-antui",
  "version": "0.3.44",
  "description": "Extends mini-antui.",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "cp -r app/dev dist",
    "pub": "npm run clean && npm run build && npm publish"
  },
  "author": "fallenink",
  "license": "ISC",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
